# Default ignored files
/shelf/
/workspace.xml
# Ignored default folder with query files
/queries/
# Datasource local storage ignored files
/dataSources/
/dataSources.local.xml
# Editor-based HTTP Client requests
/httpRequests/

git push origin
git push gitea

git push origin main
git push gitea main

# Before starting work each day:
git fetch origin
git fetch gitea
git status

# After making commits, push to both remotes:
git pushall
# (This runs: git push origin main && git push gitea main)

# If you get divergence issues in the future:
git pull --rebase origin main    # For origin conflicts
git push --force-with-lease gitea main    # If gitea falls behind